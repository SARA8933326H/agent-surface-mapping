{
  "name": "@surface/classifier",
  "version": "0.0.1",
  "description": "Heuristic + optional LLM classifier and risk mapper for attack surface discovery.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@surface/shared": "workspace:*",
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
